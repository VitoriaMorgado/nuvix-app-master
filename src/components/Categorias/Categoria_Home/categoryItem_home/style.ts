
import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    alignItems: "center",
    padding: 16,
  },
  categoryColumn: {
    flex: 2,
    flexDirection: "row",
    justifyContent: "space-around",
  },
  categoryText: {
    fontSize: 14,
    color: "#ffffffff",
  },
});
